export const svgColors: Record<string, { light: string; dark: string }> = {
  defaultLight: { light: "#98A2B3", dark: "#4B5563" },
};

export const DefaultColors: Record<string, { light: string; dark: string }> = {
  background: { light: "#ffffff", dark: "#111827" },
};

export const storageKey = "vite-ui-theme";